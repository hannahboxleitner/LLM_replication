---
title: "Term paper: Replication of an LLM study by Morin & Larsson (2025)"
author: Hannah Boxleitner
format: html
bibliography: references.bib
---

# Introduction

In the last decades, Researchers in corpus linguistics gained access to increasingly massive corpora of natural language in different languages. This poses great opportunities, but also necessitates manual cleaning and processing of large amounts of data [@morin2025: 1]. The rise of large language models (LLMs) is therefore a rather interesting development for corpus linguistic research, making it possible to handle larger datasets and corpora automatically. In recent years, many papers testing LLMs for assisting in linguistic research have been published **(sources)**.

I aim to replicate a study by @morin2025, which presents a replicable method for leveraging LLMs for assistance in linguistic grammatical annotation. While the study was conducted with the model Claude Sonnet 3.5, I will attempt to replicate it using the same data, but with an open LLM (Mistral) in R.

This project is intended to embody the principles of Open Science in two respects: First, I want to contribute to the use and examination of open LLMs for analysis of linguistic data. While closed LLMs (like ChatGPT) are widely used in day-to-day life, their usage brings difficulties regarding data rights, privacy, etc. **(see presentation for citation)** among others. Many open alternatives are on the rise, but are not as widely used and investigated in studies as necessary **(source?)**. **Second**, I want to conduct a replication and thereby participate in the central aim of making linguistic research more cumulative and robust.

# Methodological approach

In planning this replication, I used the handbook by @röseler2025 as a guide for the research process. On a general note, the following steps are listed (and conducted in this replication study):

-   choosing a target study,

-   deciding between different types of replications,

-   planning and running the new study,

-   analyzing the results,

-   discussing outcomes in the light of potential differences,

-   and publishing a report [@röseler2025].

# The target study

The target study by @morin2025 focuses on the verb *consider*, which can be found in a number of larger grammatical constructions, one of which is an evaluative use introducing an evaluation or judgement.

**insert screenshots p. 2**

The relationship between the three variants is unclear and it has been suggested that the *consider + as* construction might be becoming less and less common. Searching for this construction in the EnTenTen21 corpus (**source**), the authors encountered the problem that the evaluative construction coexists with another prominent use of consider as a cognition verb **(include example?)**, and out of a random sample of 200 CONSIDER tokens, only 11.5% were found to be true positives and therefore of interest for the research question [@morin2025: 2]. For a corpus-based study addressing this methodological challenge, @morin2025 make use of LLMs for cleaning and annotation tasks, and outline how to automatically annotate large quantities of evaluative *consider* constructions, based on the key processes of prompt engineering, training, and evaluation. For this task, the LLM Claude Sonnet 3.5 and data from the NOW corpus ans Sketch Engine's EnTenTen21 corpus was used [@morin2025: 3]. **short summary of the study and the results??**

Include source for the dataset that can be downloaded (see project from last year to mmake it look nice)

# Planning the replication

## Decide on type of replication

**close replication?** [@röseler2025: Chapter 4] In planning and defining the terms for this project, I encountered some problems with the terminology of replication research. Many things that need to be considered for reproductions or replications are usually formulated with respect to experiments with participants, where sample sizes etc. matter. Replications are also often defined as repeating a study with different data, for example **(source: table in Röseler**). For this project, a replication of a study that leverages LLMs to assist in grammatical annotation for linguistic research, a lot of the terminology does not fit or apply. While I intend to use the same data as @morin2025, which I intend to replicate with another LLM, one can argue that using different models creates different data. Also, the models somehow represent participants in this setting, since they basically replace annotators who would otherwise do this task manually. *maybe aufgreifen in the conclusion!* *entweder hier noch auf data leakage eingehen oder später* Therefore, I had to adapt the theoretical framework as possible, and go from there. Since I plan on replicating @morin2025's approach very closely, use the data that is provided, and also adapt their prompting very closely, this project would probably be considered a *close replication*. Achieving a very close resemblance with the original study can be difficult for multiple reasons. Among others, original studies may not include sufficient detail in some respects [@röseler2025: Chapter 6.3]. In the case of replicating @morin2025, the authors share their data and a transcript of the conversation, while also explaining their approach in the paper, but the code with which the automated pipeline was implemented (or even the programming language, for example) is not shared. This is an important aspect to consider, since resembling the study in terms of implementing the pipeline itself is only possible to a limited extent.

## Planning to replicate the method and piloting

To plan my replication, I read the study by @morin2025 thoroughly, especially focusing on the pre-training and prompt engineering.

Further insight could be gained by going through the conversation transcript that @morin2025 provided with their data. It lists user prompts and answers of the model in a `docx.`-file. The conversation consists of the following parts:

**Pretraining**

-   First, the LLM gets a user prompt containing detailed instructions on how to classify the sentences, and is presented with examples for every instance and a step-by-step approach to classification.

-   It is instructed to look at 493 pretraining sentences in a dataset containing correct classifications, think about each classification step-by-step, and encouraged to ask questions.

**Training**

-   A round of supervised classifications follows: The model is prompted to classify the first 100 sentences in batches of 25. Mistakes are corrected through user prompts.

-   After that, another dataset of 100 sentences is requested to be classified without the close supervision (and to be provided in tab-separated .csv-format).

-   All incorrect classifications are pasted into the next user prompt. The model is told its accuracy for the task and instructed to review the errors.

-   **These reviews are not further commented,** the model is simply told to keep lessons in mind. It is requested to classify step-by-step and consistently apply the insertion test, then given new data (EnTenTen_random_sample_101.csv), and told to do the full classification in one batch in .csv format.

-   It is told the accuracy rate (93%) and told it is good

**Testing**

-   Provided with new data (EnTenTen_random_sample_102.csv) and told to follow the procedure that was successfully applied in the latest round of classifications. After classification, it is again told the accuracy.

## Piloting

Since this approach is quite extensive and detailed, and there is no code to look into for aspects like e.g. how the datasets were implemented in the prompts, I decided to conduct a little pilot study to test a similar multi-turn-conversation.

I used a small dataset I had annotated manually once for another project. The task was a sentiment analysis with a lot less detailed instructions. The goal of this pilot was not to follow @morin2025's approach exactly or to achieve a high accuracy, but rather to implement the steps of pretraining, training and testing and make sure the model can access the datasets. In the last round of testing, I actually overlooked one step of informing the model about the "new" accuracy of corrections, which might have improved accuracy in the end. This made me further aware of the exact process and glad I conducted a pilot study.

## Deciding on a model

For this project, I wanted to replicate the study by @morin2025 with a different, and especially an open LLM for the task. Since running LLMs locally was only possible with rather small LLMs due to the available hardware, I used an API key via KI:connect.

The model chosen for this project is Mistral Small 3.2 24B Instruct 2506, which was released in 2025 under Apache 2.0 license. It is a minor update of [Mistral-Small-3.1-24B-Instruct-2503](https://huggingface.co/mistralai/Mistral-Small-3.1-24B-Instruct-2503), improved in the area of instruction following, repetition errors, and function calling. Even though this model is considered open source, one might take a closer look in terms of openness: It is evidently open in some respects (open weights, for example), but closed in others (e.g. no open code) [@mistrala]. However, considering available options for this project and that a certain size and performance was required for the task at hand, it is still the most suitable choice.

Using the API documentation in [@ki:conne], I was able to get started on chat completions.

Considering the concern of data leakage, it is important to note that the model was released in summer of 2025. **When it was trained exactly is unclear.** @morin2025, who used data from the NOW corpus and Sketch Engine’s EnTenTen21 corpus [@morin2025: 3], created their OSF-database also containing the annotated dataset used as the gold standard for comparison, in November 2024.

## Defining replication criteria and hypothesis

@morin2025 present a methodological pipeline leveraging an LLM for the automatic annotation of grammatical data, based on the case study of the evaluative *consider* construction in English. Their research questions are concerned with the frequency of the different variants of the *consider* construction. While they don't formulate a hypothesis in their paper, they state a twofold goal to 1) build a replicable methodological pipeline that can be repurposed and 2) to substantially improve the quality of annotation of future corpus studies [@morin2025:3].

In view of the first goal, this paper will focus on the replication process itself. My research question is: Is this study replicable with another, open LLM? **While they mention generalizability towards other languages or other grammatical constructions, generalizability considering other models is a highly relevant question.**

To answer this research question, I need to make The authors state in their abstract to have reached "a model accuracy of over 90%" [@morin2025: 1]. While they include some other evaluation metrics, they mostly focus on accuracy in the rest of the paper. For the replication of this project, I therefore want to define reaching an **accuracy of over 90%** as the main **criterion** for a successful replication.

While attempting to reach this goal, the focus of this project - **with regard to principles of open science** - remains the replication process itself, which, as mentioned before, is supposed to be as close as possible to the methodological pipeline presented in @morin2025, including specifics like

-   using XML-tags in user prompts (e.g. for \<instructions\> classification instructions \</instructions\>)

-   putting all the instruction inside user prompts (as suggested in the transcript) as opposed to implementing a system prompt

-   etc.

Based on these considerations, I want to establish the following hypothesis for this project: Using the methodological pipeline and data from @morin2025 in a close replication with the open model Mistral Small 3.2 24B Instruct 2506, it is possible to reach an accuracy level of over 90%.

**Relevant** **to your field of study and/or society more broadly**

# Running the replication

(Via the KI:connect API documentation <https://chat.kiconnect.nrw/app/api-docs/#tag/chatcompletions/post/v1chatcompletions>, I set up the LLM for usage with an API key, and tried out some prompts (use what for formulating prompts?). **Integrating the iterative nature of a multi-turn-conversation posed a challenge. Reading up on OpenAI's Conversation API <https://developers.openai.com/api/docs/guides/conversation-state>, which unfortunately does not exist for this model** (?)**, reading up on packages suited / using these packages, I tried to build something similar.) -\> already in the planning section**

**The code was modified to be suitable for multi-turn conversations. For methodological consistency, I tried to keep the process as close to the one shown in the transcript of the conversation in @morin2025's data. For example, while the very long initial user prompt (containing detailed instructions for step-by-step classification with examples) could also have been implemented as a system prompt in the code, I also included it in the chat, as proposed by @morin2025.**

**testing with smaller things -\> maybe do a test run with my own dataset: things I did differently in the pilot**

-\> e.g. I did not define the desired accuracy level -\> will do in the study.; the instructions were way less detailed, the kind of classification task was different, etc.

# Results of the replication

accuracy

accuracy compared to the original study

what went wrong or well

Replication focus: Hypothesis, was the replication successful?

is the approach indeed replicable?

# Discussion of outcomes in light of differences

Relevance!

Generalizability, Opportunities

# Reflection of methodological approach and difficulties

# Conclusion

How do open LLMs perform in comparison and why is it crucial that we support and optimize them?
